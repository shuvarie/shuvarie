# AGENTS.md

Shuvarie (シュヴァリエ, "chevalier") is a terminal-based AI agent coding tool built in Rust. This file orients AI agents (and humans) to the codebase, conventions, and intended architecture so that changes stay consistent.

## Workspace crates

- `./` (binary `shuvarie`) — TUI rendering, event loop, input handling, channel wiring. **No business logic lives here.** Renders state produced by `shuvarie-core`.
- `./crates/core/` (`shuvarie-core`) — App state, config + connections loading, the async core task that orchestrates LLM and database work, and the seven agent tools (`tools.rs`: read/write/edit files, run commands, list dirs, grep, `lsp`). Owns the domain `Session`/`Event`/`Command` logic and persists through `shuvarie-db`. Depends on `shuvarie-catalog`, `shuvarie-db`, `shuvarie-llm`, and `shuvarie-lsp` (`connections.rs` holds a `shuvarie_catalog::Provider`; tools implement `shuvarie_llm::Tool`; the core task owns an `shuvarie_lsp::LspManager`).
- `./crates/catalog/` (`shuvarie-catalog`) — The uniform source of provider + model info: the `Provider` enum (9 variants) with metadata (display name, requires-api-key, default base URL), `ModelInfo`, `TokenUsage` (pure data — no rig types), and embedded data files (`data/models.toml`: unified `[models.*]` stats — context length + input/output rates per canonical `<org>/<model>` id, orgs matching Hugging Face; `data/providers/*.toml`: one file per provider with fallback rates, `aliases` from provider-specific ids to canonical ids, and `variants` — kind-labeled deployment tiers like `pro`/`thinking` with optional override rates/context), plus `resolve`/`estimate_cost`/`enrich`/`variants` (exact then longest-prefix alias matching, provider-specific overrides win, provider-default fallback for unknown ids, `enrich` fills missing runtime context lengths). Only serde + toml. No TUI concerns.
- `./crates/llm/` (`shuvarie-llm`) — Thin wrapper over `rig`: `ProviderClient` builder, model listing (via rig's `ModelListingClient`), streaming agent chat (`stream` — multi-turn with tools; `run_worker` — single-shot worker agent loop), the portable `Tool` trait + `ToolDefinition` + `ToolOutput` (the boundary types `shuvarie-core` implements; a tool returns `ToolOutput { text, file_change }` where `file_change` is an optional `FileChange` — `Edit { path, diff, original, new }` or `Write { path, content, original }` — and rig `DynamicTool`s are built inside `shuvarie-llm`, which forwards the `file_change` through a side channel into the emitted `StreamItem::ToolResult`), `WorkerAgent` (an agent-as-tool whose `call` runs its own `stream_chat` loop over a narrowed tool set, forwarding its tool activity through an mpsc channel merged into the manager stream), `ChatMsg`/`Role` message types, `StreamItem` (with `Delta`/`Reasoning`/`ToolStart`/`ToolResult`/`WorkerStart`/`WorkerResult`/`Done`/`Error` — reasoning captured from rig's `StreamedAssistantContent::Reasoning`/`ReasoningDelta`), and rig→catalog conversions (`model_info_from_rig`, `token_usage_from_rig`). Depends on `shuvarie-catalog` for the shared types; no pricing/estimation lives here. No TUI concerns.
- `./crates/db/` (`shuvarie-db`) — Persistence layer: Toasty models (`Session`, `Message`), the `Store` wrapper over `toasty::Db` (Turso embedded SQLite via `toasty-driver-turso`), embedded schema migrations, and a `migrate` bin (toasty-cli) for managing them. No TUI concerns.
- `./crates/highlight/` (`shuvarie-highlight`) — Markdown + code highlighting for the chat pane. `md.rs` walks pulldown-cmark events into styled ratatui `Line`s (paragraphs, lists, quotes, tables, code fences); `syntax.rs` highlights fenced code via syntect with a programmatic heraldic theme (regex-fancy, no onig); `diff.rs` is a custom ` ```diff ` renderer (`+` SUCCESS, `-` ERROR, `@@` ACCENT); `theme.rs` holds the syntax color palette (a heraldic complement to `src/tui/theme.rs`). Exposes `render(text) -> Vec<Line<'static>>` and `plain(text)`. Emits unwrapped lines — wrapping/line-counting stays with the `Paragraph` in the TUI. No TUI concerns.
- `./crates/lsp/` (`shuvarie-lsp`) — Language Server Protocol client lifecycle. `config.rs` defines `LspConfig`/`LspServerSpec` (serde, `[lsp]` section of `config.toml`); `registry.rs` holds the built-in per-language server table (`rust-analyzer`, `gopls`, `clangd`, `pyright`, `typescript-language-server`, `zls`, `lua-language-server`, `solargraph`) + `merge_overrides` (user `[lsp.servers.<lang>]` overrides win field-by-field) + `probe` (PATH lookup); `server.rs` spawns one LSP server as a `tokio::process::Command` child, wires its stdout/stdin through `tokio-util::compat` into `async_lsp::MainLoop::run_buffered`, and exposes `did_open`/`did_change`/`did_close`/`shutdown`/`kill`; `manager.rs` (`LspManager`, wrapped `Arc<Mutex<..>>` as `SharedManager` in `shuvarie-core::lsp_manager`) owns the active servers, dispatches `start`/`stop`/`restart`/`list`, auto-starts on `on_file_open`/`on_file_change` (gated by `[lsp].enabled` + per-spec `auto_start`), and collects `publishDiagnostics` into a per-path `DiagnosticMap` pumped by the core task. Depends on `async-lsp` (`omni-trait`/`client-monitor`/`tracing`), `lsp-types`, `tokio`/`tokio-util` (compat), `tower`. No TUI concerns.

When adding a feature: put domain logic in `shuvarie-core`, provider/model data + pricing in `shuvarie-catalog`, LLM/SDK glue in `shuvarie-llm`, storage/ORM glue in `shuvarie-db`, markdown/code highlighting in `shuvarie-highlight`, LSP client lifecycle in `shuvarie-lsp`, and only rendering + input dispatch in the root binary.

## Architecture

### The Elm Architecture (TEA) — hierarchical

The TUI follows `map_event → message → update → return`:

- `map_event` (pure, no side effects): maps a terminal `Event` (or an async channel message) to a message (e.g. `AppMessage`). Takes `&self`/`&App` only — never mutates.
- `update` (mutates the model): consumes a message and may produce an effect (e.g. `AppEffect::Quit`) or an effect for the parent.
- `view(&self, frame: &mut Frame<'_>, area: Rect)`: draws current state. A TEA model is **not** required to implement `ratatui::Widget`; the dedicated `view` method takes a `Frame` + `Rect` and may compose multiple widgets, so it is not limited to a single widget's render contract. No side effects in `view`.

The root `App` composes **submodels**, each following the same TEA shape with its own message enum, `map_event`, `update`, and `view`. Submodels own their state and logic; the parent dispatches events, forwards grouped messages, handles routing, and processes core events:

```
App (parent)
├── HomeScreen          — HomeMessage, map_event(&self), update → Option<HomeEffect>, view(&self)
├── SessionScreen       — SessionMessage, map_event(&self), update → Option<SessionEffect>, view(&self)
├── CommandMenu         — CommandMenuMessage, map_event, update → Option<CommandMenuEffect>, view(&self)
├── AddProviderForm     — AddProviderMessage, map_event(key, stage), update → AddProviderOutcome, view(&self)
├── ModelPicker         — ModelPickerMessage, map_event, update → Option<ModelPickerEffect>, view(&self)
├── Welcome             — WelcomeMessage, map_event, (no update — parent handles directly), view(&self)
└── ConfirmQuit         — ConfirmQuitMessage, map_event, (no update — parent handles directly), view(&self)
```

When creating a new component, it's usually expected to be a TEA model, unless such an architecture doesn't fit the requirement.

There should be a `new` and a `view` method, and a `update` method when data updating is required.

**Module layout** (`src/tui/`):

| File | Role |
|---|---|
| `tui.rs` | Event loop: `EventStream` + `tokio::select!`, terminal init/deinit, calls `App::view`. Coalesces bursts of core events and bounds redraws to a configurable frame rate (`[ui].frame_rate`, default 60; `0` = draw every event); terminal events draw immediately |
| `event.rs` | `Event` enum — `Terminal(termina::Event)` / `Core(shuvarie_core::Event)` — the single input type for `App::map_event` |
| `app.rs` | Parent `App`: `Route` (`Home`/`Session`), `Overlay` enum, `AppMessage` (grouped), `map_event` dispatch, `update`, `view` (content routing + overlays) |
| `context.rs` | `UpdateCtx` — shared `Connections` + `Command` sender passed to submodel `update` calls |
| `home.rs` | `HomeScreen` submodel + `HomeMessage` + `HomeEffect` (ASCII art + input) |
| `session.rs` | `SessionScreen` submodel + `SessionMessage` + `SessionEffect` (sidebar + chat history + input) |
| `sidebar.rs` | `Sidebar` view widget (Shuvarie+version, Context, LSP, Skills) |
| `logo.rs` | Double-sword-behind-shield ASCII art for the Home screen |
| `add_provider.rs` | `AddProviderForm` submodel (two-stage wizard: SelectKind → Details) + `AddProviderMessage` + `AddProviderOutcome` |
| `model_picker.rs` | `ModelPicker` overlay submodel + `ModelPickerMessage` + `ModelPickerEffect` |
| `session_picker.rs` | `SessionPicker` overlay submodel (session list, resume, `Ctrl+D` delete with confirm) + `SessionPickerMessage` + `SessionPickerEffect` |
| `command_menu.rs` | `CommandMenu` submodel + `CommandMenuMessage` + `CommandMenuEffect` |
| `welcome.rs` | `Welcome` overlay (first-run) + `WelcomeMessage` |
| `confirm_quit.rs` | `ConfirmQuit` overlay (Ctrl+C) + `ConfirmQuitMessage` |
| `history_search.rs` | `HistorySearch` overlay submodel (Ctrl+R full-text search over session messages, Enter jumps to the found session) + `HistorySearchMessage` + `HistorySearchEffect` |
| `approval.rs` | `ApprovalPrompt` overlay submodel (Approve / Always approve / Deny, `←`/`→` focus, Enter/Esc) + `ApprovalMessage` + `ApprovalEffect` |
| `search.rs` | `Search` — nucleo fuzzy `filter_indices` helper |
| `list.rs` | Stateless list rendering helpers — `scroll_offset_for` (scroll-into-view computed in `update`), `render_list_item`/`render_list_item_line` (bake selection `▶ ` prefix + `ACCENT_BG` row style into `ListItem`) |
| `components.rs` | Shared input widgets — `InputBuffer` (char buffer + cursor, Emacs movement, virtual cursor rendering), `TextArea` (message input with submit effect) |
| `theme.rs` | Color palette + style helpers (see UI design below) |
| `escape.rs` | CSI escape sequences for alt-screen enter/exit and Kitty keyboard protocol enable/disable |

**Message grouping**: `AppMessage` wraps submodel messages — `Home(HomeMessage)`, `Session(SessionMessage)`, `AddProvider(AddProviderMessage)`, `ModelPicker(ModelPickerMessage)`, `CommandMenu(CommandMenuMessage)`, `Welcome(WelcomeMessage)`, `SessionPicker(SessionPickerMessage)`, `HistorySearch(HistorySearchMessage)` — plus parent-only variants (`OpenCommandMenu`, `RequestQuit`, `ConfirmQuit`, `CancelQuit`, `Resized { rows, cols }`, `ConfigSaved`, `ConfigError`, `ModelsLoaded`, `ModelsError`, `SessionsLoaded`, `SessionLoaded`, `SessionCreated`, `SessionDeleted`, `SessionError`). The TUI `Event` enum (`src/tui/event.rs`) unifies terminal and core inputs: `Event::Terminal(termina::Event)` and `Event::Core(shuvarie_core::Event)`. `App::map_event(ev, &app)` matches on `Event` — terminal events are dispatched overlay-aware then route-aware (below), while core events are mapped to `AppMessage` directly: stream/usage events (`TokenReceived`, `StreamDone`, `StreamError`, `StreamCancelled`, `UsageUpdate`) wrap into `Session(...)`, session events (`SessionLoaded`, `SessionCreated`, `SessionDeleted`, `SessionsLoaded`, `SessionError`) stay at the parent level (they populate the chat view and the SessionPicker), search events (`SearchResults`, `SearchError`) wrap into `HistorySearch(...)`, `Pong` closes the command menu, and `ConfigSaved`/`ConfigError`/`ModelsLoaded`/`ModelsError` stay at the parent level (the parent reloads config from disk on `ConfigSaved` so submodels see fresh provider data, and auto-selects a model on `ModelsLoaded`). `Resized` is sent from the `tui.rs` event loop when the terminal size changes and dispatched in `App::update` to the open overlay's `Resize { viewport_height }` submessage (computed from the popup rect + overlay block inner layout) so the overlay can recompute its scroll offset.

**Event dispatch flow** (`App::map_event`, overlay-aware, then route-aware):

1. `Event::Terminal` — if an overlay is open → route to that overlay's `map_event` (CommandMenu, AddProviderForm, ModelPicker, Welcome, ConfirmQuit, SessionPicker, HistorySearch).
2. Else `Ctrl+C` → `RequestQuit` (opens ConfirmQuit overlay; cancels the stream first if the session is streaming).
3. Else `Ctrl+M` → `OpenCommandMenu` (parent-level global keybind).
4. Else `Ctrl+R` (on the Session route) → opens the `HistorySearch` overlay.
5. Else route to the active screen's `map_event` (`Home` or `Session`).
6. `Event::Core` — mapped to `AppMessage` directly (see Message grouping above).

**Update forwarding**: `App::update` matches `AppMessage` — parent-only variants are handled in-place; submodel variants are forwarded to `submodel.update(msg, &self.ctx)`. `CommandMenu::update` returns `Option<CommandMenuEffect>`; the parent matches effects (`OpenModelSelect`, `AddProvider`, `OpenSessionPicker`, `NewSession`, `UndoLastTurn`, `Redo`, `Replay`, `Resume`) to perform parent-level actions (opening the ModelPicker overlay, opening the AddProviderForm, opening the SessionPicker / starting a fresh session, or sending the corresponding `Command` to core). `HomeScreen::update` returns `Option<HomeEffect>` (`Submit` → start session); `SessionScreen::update` returns `Option<SessionEffect>` (`SendMessage` → send command to core). `AddProviderForm::update` returns `AddProviderOutcome` (`Submit` → add provider + set active + list models; `Cancel` → close or back to kind list). `ModelPicker::update` returns `Option<ModelPickerEffect>` (`Selected` → set active model). `SessionPicker::update` returns `Option<SessionPickerEffect>` (`LoadSession` → resume; `DeleteSession` → delete with a second `Ctrl+D` to confirm; `NewSession` → fresh conversation). `HistorySearch::update` returns `Option<HistorySearchEffect>` (`Open` → show overlay; `Search { query }` → send `Command::SearchHistory`; `LoadSession` → resume the found session).

**`UpdateCtx`** (`src/tui/context.rs`): carries `connections: Connections` and `cmd_tx: Sender<Command>`. Passed by shared reference (`&UpdateCtx`) to submodel `update` calls so they can read provider data and send commands without owning them. The parent owns the `UpdateCtx` and reloads `connections` from disk on `ConfigSaved`.

**Adding a feature**: add a message variant to the relevant submodel's enum (and an effect/return if the parent needs to act), an `update` arm, and `view` logic. Keep side effects out of `map_event` and `view` — send commands via `ctx.send(...)` in `update` only. If a new submodel is needed, add a message enum, `map_event`, `update(&UpdateCtx)`, and `view`, then compose it in `App` and add a grouped `AppMessage` variant.

The `view` method must have an immutable `self` reference (`&self`) as parameter — it never mutates the model.

### Async runtime: Tokio

`main` is `#[tokio::main]`. The TUI loop runs on the main thread using `termina`'s `EventStream` (the `event-stream` feature) so the loop can `tokio::select!` between terminal key events and core events without blocking. All LLM and database work runs on a spawned **core task** (`shuvarie_core::run`). Communication is via `tokio::sync::mpsc` channels:

- TUI → core: commands (e.g. `SendMessage`, `StartSession`, `ListModels`, `AddProvider`, `SetActiveModel`) — `shuvarie_core::Command`.
- Core → TUI: events (e.g. `MessageReceived`, `ReplyError`, `SessionStarted`, `ModelsLoaded`, `ConfigSaved`) — `shuvarie_core::Event` — wrapped in `Event::Core` by the `tui.rs` event loop, mapped to `AppMessage` variants by `App::map_event`, and fed into `update`.

Keep the TUI thread free of `await`s on blocking work; offload any blocking work to the core task (or `spawn_blocking`). The `select!` loop wakes on either a terminal event or a core event, so live updates (model lists, streaming tokens) render without requiring a keypress. Core events are coalesced: after a core event, all already-queued core events are drained and applied, then at most one frame is drawn per frame-budget interval (`[ui].frame_rate`, default 60 fps; `0` disables the cap). Terminal events draw immediately for input responsiveness. When no event changed state, the loop keeps listening without redrawing.

### Storage: hybrid

- **Config file** (`~/.config/shuvarie/config.toml`, via `toml` + `serde`): UI preferences (`[ui].frame_rate` — target frames per second for the render loop, default 60; `0` draws every event), the `[embedding]` settings, the `[agent]` turn budgets (`max_turns` default 30, `worker_max_turns` default `0` = unlimited), the `[context]` context-management settings (see Context management below), the `[lsp]` settings (`enabled` defaults to true; `[lsp.servers.<lang>]` overrides or adds server specs — `command`, `extensions`, `auto_start`, `root_markers`), and the `[skills]` settings (`enabled` defaults to true; `dirs` — extra skill directories). No secrets.
- **Connections file** (`~/.config/shuvarie/connections.toml`, via `toml` + `serde`): provider connections (API keys, base URLs) and the active provider/model. Keeps secrets out of the database and out of `config.toml`.
- **Database** (`.shuvarie/data.db` in the working directory — Turso embedded SQLite + Toasty ORM): chat sessions, message history (with reasoning text + interrupted flag), tool call records (per-message, with file-change JSON + original/new content for undo), and the undo log (DB-backed multi-level redo, survives reload). Provider config stays in the TOML files regardless.
- **Migrations**: schema is managed with toasty migrations, not `push_schema` (which emits bare `CREATE TABLE` and fails on re-open). Migration files live in `crates/db/toasty/` and are embedded into the binary via `toasty::embed_migrations!`; `Store::open` applies them automatically. Regenerate with `cargo run -p shuvarie-db --bin migrate -- migration generate --name <change>`, then `apply` (or just reopen a store — the embedded set applies pending migrations at open). The `migrate` bin uses an in-memory DB and a programmatically-built `toasty-cli` config pointing at `crates/db/toasty` so it is CWD-independent. `next_seq` uses `MAX(seq)+1` (not row count) so undo/delete doesn't collide.
- **Tool call + reasoning persistence** — each assistant turn's tool calls (name, args, output, ok, worker, file-change JSON, original/new content) are persisted in a `tool_calls` table keyed to the assistant `Message` row (created eagerly on first tool start of the turn, updated on `Done`/`Error`/cancel). Reasoning/thinking text is captured from rig's `StreamedAssistantContent::Reasoning`/`ReasoningDelta` and stored in `messages.reasoning`. On session reload, `Session::from_stored` reconstructs tool records + reasoning so the chat pane shows full tool activity and diffs, not just plain text. `interrupted: bool` on `Message` marks turns cancelled by the user or aborted by errors (including the max-turn limit) — the TUI keeps interrupted turns visible (the "Resume" command is offered only when the last assistant message is interrupted).
- **Undo / redo / replay / resume** — `Command::UndoLastTurn` moves the last user+assistant pair + their tool calls into the `undo_log` table and reverts file changes (restoring `original_content` or deleting newly-created files). `Command::Redo` pops the last `undo_log` row, re-inserts messages + tool calls, and re-applies file changes. `Command::Replay` = undo last turn + re-`SendMessage` the last user content as a fresh turn. `Command::Resume` = delete the interrupted assistant message + its tool calls, then re-stream from the last user message (removing the incomplete turn, restarting it). The TUI keeps interrupted turns visible in the chat (partial text + reasoning + completed tool activity; still-running tools with no result are dropped) — the incomplete turn is only removed when rerun via `Replay`/`Resume`. `SendMessage` truncates the session's `undo_log` (a new turn invalidates redo). Undo reverts file changes only from `read`/`write`/`edit`/`list_dir`/`grep` — `run_shell` effects are never reverted.
- **Full-text search** — the schema declares `CREATE INDEX "index_messages_fts" ON "messages" USING fts ("content")` (Turso/Tantivy FTS), and `Store::open`/`open_in_memory` construct the driver with `.experimental_index_method(true)` so that statement is accepted. Searches run through toasty's raw SQL (`toasty::sql::query` with `fts_match`/`fts_score`, `?1`-style numbered placeholders, `column_types` hints for decoding) in `Store::search_messages` — BM25-ranked hits joined with session titles, content returned raw for the TUI to truncate. The FTS index tracks inserts/deletes automatically.
- **Semantic search** — a `message_embeddings` table stores one `vector32` blob per message (raw f32 LE bytes in a `Vec<u8>` BLOB column). Embeddings are written on message append and backfilled at startup (batches of 64) via `Store::upsert_embedding`/`messages_missing_embeddings`; `Store::semantic_search` ranks by `vector_distance_cos(e.vec, ?1)` (bind the blob with `bind_typed(..., db::Type::Blob)`). Deleting a session cascades through Toasty's app-level cascade (required FK `message_id: u64` on `MessageEmbedding` → `Message`, chained from `Session`) — no manual deletes. `Ctrl+R` search is hybrid: FTS results render instantly, then a debounced (300 ms, abort-on-keystroke via `AbortHandle`) query embed runs `semantic_search` and both lists merge by reciprocal rank fusion (`rrf_merge`, k=60) — semantic hits carry `SearchSource::Semantic` (`⌕ ` tag in the overlay). Embedding config lives in `[embedding]` (provider/model/dimensions; defaults per provider kind, see `shuvarie-core::embeddings`); providers without embedding support (Anthropic/DeepSeek/Groq) degrade to FTS-only.
- **Context management** — the multi-turn agent loop re-sends the full accumulated conversation (preamble + history + every prior tool result) on each model call, so long turns blow up input tokens quadratically. Shuvarie bounds this with three layers: (1) **Tool-output caps** — `read_file` truncates returned text at `[context].tool_output_max_chars` (default 16,000 chars) with a `… (use offset/limit)` marker; a per-turn `ReadCache` (`tools.rs`) dedupes repeated reads of the same `(path, offset, limit)` so the model can't re-read a 64 KB file 30×. (2) **History-budget hook** — `shuvarie-llm`'s `ContextHook` (a rig `AgentHook` in `context_hook.rs`) runs before each model call: it estimates the request size (chars/4 heuristic) and, when over the usable budget (`context_length − [context].reserved`, context from `shuvarie_catalog::resolve` or `[context].fallback_context_length` default 128K), replaces the history for that turn with a trimmed copy (`RequestPatch.history`) that keeps the recent tail verbatim and replaces old tool results with `[tool result omitted]`. Per-call usage from rig's `MultiTurnStreamItem::CompletionCall` is tracked in a `UsageTracker`; when real cumulative input crosses the budget the hook stops the run (`CompletionCallAction::Stop`), surfaced as `StreamItem::Overflow`. (3) **Compaction** (`compaction.rs`) — on `Overflow`, the older messages (head) are serialized (tool outputs truncated to 2 KB) and summarized via a single-shot `run_worker` call into a structured Markdown summary (Objective / Important Details / Work State / Next Move / Relevant Files), persisted as a `summary`-flagged `Message` (`Store::append_summary`). `Session.summary_seq` tracks the most recent summary; `Session::history_for_send` replaces everything before it with the summary on subsequent turns. The TUI renders a `◈ summary of earlier conversation` marker above the summary message. Config: `[context]` (`enabled` default true, `reserved` default 20,000, `tool_output_max_chars` default 16,000, `fallback_context_length` default 128,000).

### LLM providers

All provider access goes through `rig` in `shuvarie-llm`. Supported providers, via `rig`'s built-in clients:

- **OpenAI-compatible** — OpenAI proper plus any OpenAI-compatible endpoint (OpenRouter, local `llama.cpp` server, etc.).
- **Anthropic** — Claude models.
- **Gemini** — Google models.
- **Ollama** — local models, no API key required (good for offline development).
- **Ollama Cloud** — Ollama's hosted cloud service (`https://ollama.com`, Bearer API key from https://ollama.com/settings/keys); same Ollama API as local, so it reuses rig's `ollama` client with a key + base URL.

`shuvarie-llm` exposes a `Provider` enum and a model-listing/streaming API; the root binary never calls `rig` directly.

### Rendering stack

`ratatui` with the `termina` backend feature (`default-features = false`, curated feature set in the root `Cargo.toml`). Alternate-screen enter/exit is handled manually via CSI escapes in `src/tui/escape.rs` — do not switch to a backend-managed alt-screen toggle without reason. The Kitty keyboard protocol is enabled on init (`ENABLE_KITTY_KEYBOARD` — `DISAMBIGUATE_ESCAPE_CODES | REPORT_ALTERNATE_KEYS`) so modified keys like `Shift+Enter` carry their modifier bits (parsed via termina's `parse_csi_u_encoded_key_code`); terminals that don't support it ignore the sequence and the app degrades gracefully (the newline binding is inert, `Enter` still submits). The flags are popped on deinit.

### UI design — modern, borderless, muted

The visual style is defined in `src/tui/theme.rs` and used by all submodel `view` methods. Follow these principles when adding or modifying UI:

**Color palette** — all colors are `Color::Rgb` (true color), a medieval/heraldic theme: dark iron-and-stone backgrounds, antique gold accent (heraldic *or*), parchment-cream text, and weathered tinctures for semantics. Never use pure RGB primaries (`Color::Red`, `Color::Green`, etc.) or the `Stylize` shorthand colors (`.red()`, `.cyan()`, etc.) in UI code — import from `theme` instead. The palette:

| Constant | Role |
|---|---|
| `BG` | App background (forged iron black) |
| `SURFACE` | Unfocused pane/section background (castle stone) |
| `SURFACE_FOCUSED` | Focused pane background (polished iron) |
| `OVERLAY` | Popup/modal background (weathered stone) |
| `ACCENT` | Heraldic gold — titles, keybindings, active markers, highlights |
| `ACCENT_BG` | Selection background (dark bronze, not harsh inversion) |
| `TEXT` | Primary text in focused panes (parchment cream) |
| `TEXT_DIM` | Secondary text, unfocused pane items (faded vellum) |
| `TEXT_MUTED` | Hints, descriptions, placeholders (weathered stone) |
| `SUCCESS` / `WARNING` / `ERROR` | Heraldic tinctures: vert (sage) / amber / gules (weathered red) — never pure RGB |

**Windows and sections** — no borders. Use `theme::section_block(title, focused)` which returns a `Block::new()` with a background fill (`SURFACE` or `SURFACE_FOCUSED`) and the title rendered inside via `.title_top()` in `ACCENT` + bold. The block has `Padding::horizontal(1)` so content is inset from the bg edge. `Block::inner()` reserves the title row, so `List`/`Paragraph` content starts below the title automatically.

**Overlays** — use `theme::overlay_block(title)` for popups (command menu, add-provider form). It fills with `OVERLAY` bg and has `Padding::uniform(1)` for breathing room. `Clear` is still rendered first to wipe underlying cells before the bg fill.

**Selection** — selection is **baked into the `ListItem`** (not via `List::highlight_style`/`highlight_symbol` or `ListState`). `list::render_list_item`/`render_list_item_line` prepend a `▶ ` prefix `Span` in `ACCENT` for the selected row (`  ` for unselected) and set `ListItem::style` to `Style::new().bg(ACCENT_BG).fg(TEXT)` for the selected row (so the full row width gets the `ACCENT_BG` fill, matching the old `highlight_style` behavior) or `Style::new()` for unselected. Lists are rendered statelessly via `frame.render_widget(List::new(visible), area)` — never `render_stateful_widget`. The selected index (`selected: usize`) and scroll `offset` live as plain fields on the model, mutated in `update`. Active provider/model items use `theme::active_marker(is_active)` → `●` in `ACCENT` (distinct from the selection cursor `▶`).

**Stateless widgets & scroll-into-view** — `ratatui-widgets` implements `Widget` for `&Widget` references (e.g. `&List`, `&Paragraph`, `&Block`), so widgets are rendered by reference without state. Scroll-into-view is **not** handled by a stateful widget at render time; instead the model tracks `selected`, `offset`, and `viewport_height`, and `update` calls `list::scroll_offset_for(selected, offset, viewport_height, len)` to recompute the offset whenever selection or viewport size changes. The viewport height arrives via a `Resize { viewport_height }` submodel message that `App::update` dispatches on `AppMessage::Resized { rows, cols }` (sent from the `tui.rs` event loop when the terminal size changes, computed from the popup rect + overlay block inner layout). `view` then slices the visible items with `.skip(offset).take(list_area.height)` before building the `List`. Do not introduce `ListState`/`TableState`/`ScrollbarState` for list selection — use this pattern instead.

**Scrollable text panes** — the session chat history is the one exception to the stateless pattern: it renders a `Paragraph` (with `Wrap`) into a cached `tui_scrollview::ScrollView` buffer and keeps a `ScrollViewState` on the model. Because TEA `view` takes `&self` but `render_stateful_widget` needs `&mut state`, the state lives in a `RefCell<ScrollViewState>` (`scroll_state` on `SessionScreen`); the `ScrollView` itself is also cached (in a `RefCell`), rebuilt only when the content changes (`scroll_dirty` flag set in `update` via `mark_scroll_dirty()`) or the pane width changes — never on every frame, so scrolling stays responsive while streaming. `update` drives scrolling with `state.scroll_up()`/`scroll_down()`, and auto-follows streaming output with `if state.is_at_bottom() { state.scroll_to_bottom() }` on token/stream-done/submit (so the view only pins to the bottom while the user hasn't scrolled up). The scroll-view buffer height is computed from `Paragraph::line_count(width)` (feature `unstable-rendered-line-info`), with the rightmost column reserved for the scrollbar. The scrollbar is drawn manually (buffer cells: `█` thumb in `ACCENT` on a `TEXT_MUTED` track) — do not use `ratatui::widgets::Scrollbar`, whose position math cannot bottom-align the thumb (offset clamps at `content - viewport`, so the thumb stops short of the track end at the bottom of the scroll). Streaming is cheap by construction: committed history is rendered once into a cached `committed_scroll: RefCell<ScrollView>` (rebuilt only when `committed_dirty` is set — history changed, or the pane width changed), and each streaming frame copies those already-wrapped/styled committed cells into the combined scroll view and re-wraps only the in-flight tail message; the committed lines are also cached in `committed_lines`. The `Sidebar` similarly caches its rendered lines and rebuilds them only when a `SidebarMessage` update arrives.

**Tool activity in the chat pane** — the agent's tool calls render as compact lines inside the same scroll view, associated with the assistant message they belong to. `SessionScreen` keeps `tools: Vec<ToolActivity>` (name, args, status, output, worker, message index, file_change); `SessionMessage::ToolStarted`/`ToolFinished` append/update entries. On session reload, `Loaded`/`TurnReverted`/`TurnRestored` populate `tools` from `session.tool_records` (persisted in the DB), so tool activity and diffs survive reentry. `rebuild_scroll_view` emits each tool as a marker line (`›` ACCENT running, `✓` SUCCESS, `✗` ERROR) with the name and truncated args, plus an indented one-line output preview, followed by the assistant text. Worker-delegated activity renders hierarchically: the manager's worker call gets a `❖` ACCENT marker, and the worker's own tool lines are indented two cells beneath it (tagged by the owning worker name via the `worker` field). An empty assistant turn (tool-call-only reply) renders "(tool output only — no text reply)" instead of an invisible empty message. Interrupted turns show an "(interrupted)" marker in `WARNING` italic.

**Reasoning in the chat pane** — thinking/reasoning text captured from rig's `StreamedAssistantContent::Reasoning`/`ReasoningDelta` renders as a collapsible block above the assistant text (`⌥ thinking ▸` header in `TEXT_MUTED` italic, collapsed by default). `SessionScreen` keeps `reasoning: Vec<(usize, String)>` (per-message) and `expanded_reasoning: HashSet<usize>`; the block expands/collapses when the header is toggled. During streaming, `pending_reasoning` accumulates reasoning deltas. On `StreamDone`/`Loaded`, reasoning is stored in the `reasoning` vec. Reasoning content is persisted in `messages.reasoning` so it survives session reload.

**Diff review in the chat pane** — when a tool returns a `FileChange`, it renders inline beneath the tool line (no streaming inside the diff). `Edit { path, diff, .. }` renders a `── diff: <path>` header then each `DiffLine` with a two-column line-number gutter (`old new`, right-aligned, `TEXT_MUTED`) and a `+`/`-`/` ` marker (`SUCCESS`/`ERROR`/`TEXT_DIM`); `DiffLineKind::Ellipsis` renders as a lone `…` between hunks. `Write { path, content, .. }` renders a `── new file: <path>` header then the content with line numbers (no `+`/`-`). The diff is computed in `shuvarie-core` (`tools.rs::compute_diff` via `similar::TextDiff`, `grouped_ops(3)` + `iter_changes`), carried through `ToolOutput.file_change` → `StreamItem::ToolResult.file_change` → `Event::ToolFinished.file_change` → `ToolActivity.file_change`. The `original`/`new` content on `FileChange` enables undo/redo of file changes.

**Approval prompts** — file tools (`read_file`, `write_file`, `edit_file`, `list_dir`, `grep`) prompt for approval when the target path is outside the workspace or under a hidden path (any component starting with `.`); `run_shell` is never gated. Writes/edits inside the workspace are auto-permitted. The gate is an `ApprovalGate` (mpsc + `oneshot`) threaded into the tools; a gated tool `await`s the user's decision before proceeding (denial surfaces as a failed tool result the model sees). The core task's `run` loop `tokio::select!`s between `cmd_rx` and the approval channel, assigning ids and emitting `Event::ApprovalRequest`; the TUI's `ApprovalPrompt` overlay (Approve / Always approve / Deny, `←`/`→` to move focus, Enter/Esc) sends `Command::ApproveTool { id, approved, always }`. "Always approve" sets a per-session flag (cleared on new/load session) that auto-approves subsequent requests without prompting.

**Context loading in the chat pane** — project context files (M7.4: workspace-root `AGENTS.md` + `.shuvarie/context/` files) surface as `◈ Loaded <path>` lines in `ACCENT` + `TEXT` at the start of the in-flight assistant turn (before tool lines), matching the tool-activity pattern. `SessionScreen` keeps `context: Vec<ContextActivity>` (paths + message index); `SessionMessage::ContextLoaded` appends entries, cleared on reset/load and on stream error/cancel alongside `tools`. Only the loaded paths render — never the file contents (they go to the agent preamble in core).

**Markdown & code highlighting** — message bodies are **not** rendered line-by-line: `rebuild_scroll_view` feeds user/system/assistant body text through `shuvarie_highlight::render(content)` (pulldown-cmark events → styled ratatui `Line`s) so markdown (bold/italic/code/lists/quotes/tables) and fenced code blocks render with the heraldic syntax palette from `shuvarie-highlight`'s `theme.rs`. Fenced ` ```diff ` blocks use the custom `diff.rs` renderer (`+` SUCCESS, `-` ERROR, `@@` ACCENT); other fenced languages use syntect (`syntax.rs`, regex-fancy) with plain-text fallback; code-block lines get a `SURFACE` background tint. The renderer emits unwrapped lines — `Paragraph` wrapping + `line_count` still handle width. Streaming re-parses the full pending text per token, which is fine because the view is already fully rebuilt each token.

**Layout** — `App::view` renders the active submodel's `view` over the full area, then any overlays (no global footer; no global title bar; branding lives in the sidebar and Home logo). Each screen owns its footer row:
- `HomeScreen::view` — centered ASCII art + centered input area, with a 1-row help footer below the input (no sidebar).
- `SessionScreen::view` — left sidebar (30 cols, `SURFACE` bg) + 1-cell `BG` gutter + content pane with centered title bar atop transparent chat history + input + status row, and a 1-row footer below the input. The footer shows contextual help via `theme::help_line(...)` (keybindings as styled `Span`s: keys in `ACCENT`, labels in `TEXT_MUTED`); while streaming it swaps to stop/commands hints. Session-scoped errors (`SessionMessage::ShowError`, e.g. model listing or session errors) render in the same footer row in `ERROR` instead of the help line — there is no separate global error row.

**Pane gutter** — two-pane layouts use `Layout::horizontal(...).spacing(1)` so the `BG` shows through as a 1-cell gap between panes (visual separation without borders).

**Virtual cursor** — `InputBuffer::cursor_line(text_color, cursor_color)` builds a `Line` with the character at the cursor position rendered with `Modifier::REVERSED` (the char's bg becomes the cursor color). When the cursor is past the last character, a reversed space block is appended. All text input areas (home, session, add-provider details) use this for cursor visualization.

**Emacs keybindings** — all text inputs support `Ctrl+B`/`Ctrl+F` (char left/right), `Alt+B`/`Alt+F` (word left/right), `Ctrl+A`/`Ctrl+E` (home/end), `Ctrl+D` (delete forward), `Ctrl+H` (backspace alias), `Ctrl+K` (kill to end). List navigations support `Ctrl+N`/`Ctrl+P` (next/prev). Arrow keys and physical keys remain alongside.

**Quit** — via `Ctrl+C` → `ConfirmQuit` overlay (Enter or `Ctrl+C` again to confirm, Esc to cancel). `q` no longer quits. The command menu is opened with `Ctrl+M`.

**Help text** — use `theme::help_line(&[("key", "label"), ...])` to render keybinding rows with keys in `ACCENT` and labels in `TEXT_MUTED`, separated by spacing. Never render help as a flat unstyled string.

When adding a new screen or widget, use the `theme` helpers — do not inline `Block::bordered()`, raw `Color` values, or `Stylize` shorthand colors.

### Launch flow

On startup `shuvarie-core` loads config + connections, opens the working-directory session store (`.shuvarie/data.db`), and — when launched with `-c`/`--current` (the CLI flag threaded into `run` as `load_current`) — loads the most recent session into the chat view. By default the TUI opens to the **Home** route; `--current` resumes the last session (the existing `SessionLoaded` → `Route::Session` path). If no providers are configured, the TUI opens to the **Home** route with a **Welcome** overlay prompting the user to add a provider; otherwise it opens to the **Home** route directly (the loaded session is one `Enter` away, or via `Ctrl+M` → "Switch session"). Provider/model management is overlay-based (AddProvider wizard, ModelPicker) and reachable at any time via the `Ctrl+M` command menu. When the user submits a message from Home, the app switches to the **Session** route.

## Conventions

- **Edition 2024**, resolver v3, workspace-inherited package metadata (`version.workspace`, etc.).
- **Errors**: `color-eyre` in the binary; library crates should use typed errors (thiserror or a manual enum) rather than `eyre`.
- **Comments**: do not add comments unless explicitly requested (see project-wide convention).
- **Formatting/lint**: `cargo fmt` (defaults; `rustfmt.toml` is intentionally empty) and `cargo clippy --all-targets` must pass.
- **Commits**: follow the `Assisted-By:` trailer convention in `CONTRIBUTING.md` for AI-assisted commits. Do not commit unless explicitly asked.
- **Skills**: `.agents/skills/` contains `ratatui`, `tokio`, `turso-db`, `toasty`, and `rig` references — consult them when working on TUI rendering, async, storage, ORM, or LLM provider/agent code. The `ratatui` skill covers Ratatui 0.30.x and the Termina backend this project uses (instead of the default Crossterm backend). The `rig` skill covers Rig 0.42.x (the `shuvarie-llm` dependency) — provider clients, agents, tools, streaming, RAG, and memory; the root binary never calls `rig` directly. These skills are also loaded by the app itself (`shuvarie-core::skills` scans `.agents/skills/` + the global `~/.config/shuvarie/skills/` for `SKILL.md` files) and surfaced in the sidebar Skills panel and the agent preamble.
- **Reuse**: for logics that are commonly used in multiple places, consider making them utils or in a shared module.

## Build & test

```sh
cargo build
cargo test
cargo clippy --all-targets
cargo fmt --check
```

## Known issues to resolve

- `shuvarie-llm` implements model listing, streaming agent chat (`ProviderClient::stream` via `rig::streaming::StreamingChat`, M5/M7.1), and embeddings (`ProviderClient::supports_embeddings` + `embed(model, dims, texts) -> Vec<Vec<f32>>` via rig's `EmbeddingsClient`/`EmbeddingsBuilder`, `embedding_model_with_ndims` for Ollama; M7.6). Anthropic/DeepSeek/Groq report no embedding support.
- `shuvarie-db` implements the Toasty models (`Session`, `Message`, `MessageEmbedding`, `ToolCall`, `UndoLog`), the `Store` wrapper (open/apply embedded migrations, list/load/create/append/delete, most-recent query, BM25 full-text `search_messages` over the FTS index, `upsert_embedding`/`messages_missing_embeddings`/`semantic_search` over vector32 blobs, `append_tool_call`/`tool_calls_for_message`/`tool_calls_for_session`, `update_message`/`set_interrupted`, `append_undo_log`/`last_undo_log`/`pop_undo_log`/`truncate_undo_log`, `last_turn`/`delete_message`/`delete_tool_calls_for_message`) and the `migrate` bin (toasty-cli) for managing `crates/db/toasty/` migration files (M6, M7.5, M7.6, M8).
- `shuvarie-core` implements config + connections load/save, `has_connected_providers()`, the `Command`/`Event` enums (including `CancelStream`, stream events, `ToolStarted`/`ToolFinished`/`WorkerStarted`/`WorkerFinished`, `ContextLoaded`, `ReasoningReceived`, `SearchHistory`/`SearchResults`/`SearchError`, `ApprovalRequest`/`ApproveTool`, `UndoLastTurn`/`Redo`/`Replay`/`Resume`, `TurnReverted`/`TurnRestored`, `LspStart`/`LspStop`/`LspRestart`/`LspList`, `LspStatus`/`LspDiagnostics`/`LspError`), the in-memory `Session` struct (with `id`/`title` tracking, `tool_records`, per-message `reasoning`/`interrupted` maps), the persistence hooks into `shuvarie-db` (session row created lazily on first message; user message persisted on send; assistant message row created eagerly on first tool start, updated on `Done`/`Error`/cancel with final text + reasoning + usage + interrupted flag; tool calls persisted on each `ToolResult`; most-recent session loaded on startup with tool records + reasoning reconstructed), the core task (`run`) that orchestrates provider/model listing and streaming chat (M5, M6), the seven agent tools (`tools.rs`, M7.1: read/write/edit files, run commands, list dirs, grep, `lsp`) with an approval gate (outside-workspace/hidden-path prompts) and `compute_diff` (via `similar::TextDiff`), the worker-agent roster (`agents.rs`, M7.3), project-aware context loading (`context.rs`, M7.4: the workspace-root `AGENTS.md` is loaded once at core-task startup and cached for the session's lifetime, while `.shuvarie/context/` files load fresh on each `SendMessage` — sorted, 64 KB/file + 256 KB total caps, binary skipped — appended to the agent preamble as `=== <path> ===` sections, announced to the TUI via `Event::ContextLoaded`), context management (`compaction.rs` — tool-output caps + per-turn read cache in `tools.rs`, a history-budget rig `AgentHook` in `shuvarie-llm`'s `context_hook.rs`, overflow-triggered LLM compaction), the embedding subsystem (`embeddings.rs`, M7.6: `setup` resolves the `[embedding]` config to a client/model/dims, on-write `index_message` spawns fire-and-forget, startup `backfill` batches 64, `rrf_merge` fuses FTS+semantic hit lists), hybrid history search dispatch (`Command::SearchHistory` → FTS `Store::search_messages` emitted immediately, then a 300 ms-debounced query embed + `semantic_search` merged via RRF, capped at 50 hits, M7.5/M7.6), undo/redo/replay/resume (M8: `UndoLastTurn` reverts file changes + moves the turn to `undo_log`; `Redo` restores from `undo_log`; `Replay` = undo + re-send; `Resume` deletes the interrupted assistant message + re-streams from the last user message), and LSP integration (M9: the core task owns an `shuvarie_lsp::LspManager` wrapped in `Arc<Mutex<..>>`, shared with the file tools (`read_file`/`write_file`/`edit_file` auto-start the matching server + `didOpen`/`didChange`) and the `lsp` tool (`start`/`stop`/`restart`/`list` with `name` as exact id or substring filter + `all` flag); a 500 ms interval pumps `publishDiagnostics` into `Event::LspDiagnostics`, and status changes emit `Event::LspStatus`).
- `shuvarie-catalog` is the uniform source of provider + model info: the `Provider` enum, `ModelInfo`, `TokenUsage`, the embedded data files (`data/models.toml`: unified `[models.*]` stats — context length + per-mtok rates per canonical `<org>/<model>` id; `data/providers/*.toml`: fallback rates + `aliases` to canonical ids + `variants` — kind-labeled deployment tiers with optional override rates/context), and `resolve`/`estimate_cost`/`enrich`/`variants` (exact then longest-prefix alias matching, provider-specific overrides win, provider-default fallback for unknown ids, `enrich` fills missing runtime context lengths so the sidebar's used-context percentage and the ModelPicker's context label work with real data).
- `shuvarie-llm` streaming runs a multi-turn agent loop (`max_turns` from `config.toml` `[agent]`, default 30, `0` = unlimited) with an agent preamble supplied by the caller (the fixed orchestrator preamble plus the loaded project context, in `shuvarie-core`); `StreamItem` carries `Delta`/`Reasoning`/`ToolStart`/`ToolResult`/`WorkerStart`/`WorkerResult`/`Done`/`Overflow`/`Error`. Reasoning content is captured from rig's `StreamedAssistantContent::Reasoning`/`ReasoningDelta` and emitted as `StreamItem::Reasoning`. A `Done` with empty text after tool calls means a tool-output-only turn. Tool errors surface as JSON error objects so the TUI marks them failed while the model still sees the message. A `ContextHook` (rig `AgentHook`) trims history per model call and stops the run on context overflow (see Context management).
- `shuvarie-lsp` owns the LSP client lifecycle: `LspManager` spawns one server per language as a `tokio::process::Command` child, wired into `async_lsp::MainLoop::run_buffered` via `tokio-util::compat`. The built-in `registry::builtin` table covers `rust-analyzer`/`gopls`/`clangd`/`pyright`/`typescript-language-server`/`zls`/`lua-language-server`/`solargraph`, probed lazily on `start` (missing binaries are skipped silently); `[lsp.servers.<lang>]` in `config.toml` overrides or adds specs. `on_file_open`/`on_file_change` auto-start the matching server (gated by `[lsp].enabled` + per-spec `auto_start`) and send `didOpen`/`didChange` (full document sync). `publishDiagnostics` are collected into a per-path `DiagnosticMap` and pumped by the core task (500 ms interval) into `Event::LspDiagnostics`. The `lsp` tool exposes `start`/`stop`/`restart`/`list` (with `name` as exact id for mutations and substring filter for `list`, plus `all` to list configured-not-running servers).
- `shuvarie-core::skills` implements the skills system: `Skills::load` scans the workspace `.agents/skills/`, the global `~/.config/shuvarie/skills/`, and any `[skills] dirs` for `SKILL.md` files, parsing their YAML front-matter (`name`, `description`, `metadata.category`, `metadata.tags`) via `serde_yaml` (workspace skills win over global on name collision). Skills load at core-task startup and emit `Event::SkillsLoaded`; the sidebar Skills panel renders name + description per skill. The agent preamble lists available skills (name + description + path) so the agent can `read_file` a skill's `SKILL.md` when relevant. `[skills] enabled` (default true) gates the system. Deferred follow-ups: per-skill enable/disable toggles, skill content injection into the preamble (currently only names/descriptions/paths are listed), and a skills management UI.
- Diff review + approval prompts (M7+): `shuvarie-llm`'s `Tool` trait returns `ToolOutput { text, file_change }`; `shuvarie-core`'s `edit_file`/`write_file` attach a `FileChange` (`Edit { path, diff, original, new }` computed via `similar::TextDiff`, or `Write { path, content, original }`), forwarded through `StreamItem::ToolResult.file_change` → `Event::ToolFinished.file_change` → the TUI's inline diff renderer (line-number gutter + `…` ellipsis between hunks). The `original`/`new` content enables undo/redo of file changes. File tools gate on outside-workspace/hidden paths via an `ApprovalGate` (mpsc + `oneshot`); the core `run` loop `tokio::select!`s the approval channel and emits `Event::ApprovalRequest`, answered by the TUI's `ApprovalPrompt` overlay (`Command::ApproveTool { id, approved, always }`).
- Multi-agent orchestration (M7.3): the main agent is an always-active orchestrator; `shuvarie-core::agents` builds three `WorkerAgent`s (`explore_workspace`, `run_tests`, `edit_files`) per `SendMessage`, each running its own `run_worker` loop (`worker_max_turns` from `config.toml` `[agent]`, default `0` = unlimited) over a narrowed tool set with a role-specific preamble. Worker activity flows back through an mpsc channel per worker, merged with the manager's rig stream via `select_all` (`merge_streams` in `provider.rs`); the `worker` field on `ToolStart`/`ToolResult` tags nested activity for hierarchical TUI rendering (`❖` delegate marker, indented sub-tool lines). Worker usage accumulates into a shared `TokenUsage` folded into the session totals on `Done`.
- Cost estimates in the sidebar Context panel are computed by `shuvarie_catalog::estimate_cost` (per-model rates from the catalog, provider-default fallback for unknown ids); token counts are real `Usage` data. User-configurable rate overrides land later.
- LSP sidebar panel shows real server status (running servers with `✓`/`⟳`/`✗` markers + per-server diagnostic counts; "no servers" / "disabled" placeholders when idle). The Skills sidebar panel shows the loaded skills (name + description per skill; "none" placeholder when empty), driven by `Event::SkillsLoaded`.
- History search (M7.5/M7.6) is hybrid BM25 + semantic (`Ctrl+R` overlay, RRF-merged); deferred follow-ups: token-based chunking for long messages (8 KB cap), query-term highlighting in results, and injecting retrieved hits into the agent preamble.
- Model search uses `nucleo` (fuzzy matcher) via `src/tui/search.rs`; the Ctrl+M command menu (`src/tui/command_menu.rs`) is a small extensible registry of `CommandEntry`s.

## Tips

### AGENTS.md

When changes are made, it's a good practice to check AGENTS.md if there is stale guidance and update it.

### ROADMAP.md

See `ROADMAP.md` for the milestone plan. Update it when milestones ship or scope changes.

### In development notice

Since the project is not released yet. It's safe to update the `0000` migration directly.
